# 图片路径问题修复

## 完成时间
2026-03-01

> ⚠️ **本文档为历史修复记录**：`scripts/fixIconPaths.js` 及配套的 `scripts/revertIconPaths.js` 属于当时的一次性迁移工具，
> 已于 2026-08 的「scripts 清理」中删除（当前图标路径规则已固化，无需再迁移）。
> 以下命令仅供参考历史修复过程，**请勿再执行**。

---

## 问题描述

物品详情页和列表页的图片无法显示，浏览器控制台显示 404 错误。

---

## 问题原因

数据文件中的图片路径与实际文件结构不匹配：

**数据中的路径格式**：
```
/icons/ds1/rings/100.webp
/icons/ds1/weapons/50.webp
/icons/ds2/armors/200.webp
```

**实际文件结构**：
```
/icons/ring1/100.webp
/icons/weapon1/50.webp
/icons/armor2/200.webp
```

---

## 解决方案

### 1. 创建路径修复脚本

创建 `scripts/fixIconPaths.js` 脚本，批量修复所有数据文件中的图片路径。

**路径映射规则**：

| 数据中的路径 | 实际路径 |
|------------|---------|
| `/icons/ds1/rings/` | `/icons/ring1/` |
| `/icons/ds1/weapons/` | `/icons/weapon1/` |
| `/icons/ds1/armors/` | `/icons/armor1/` |
| `/icons/ds1/items/` | `/icons/item1/` |
| `/icons/ds1/magics/` | `/icons/magic1/` |
| `/icons/ds1/dialogues/` | `/icons/dialogue1/` |
| `/icons/ds2/rings/` | `/icons/ring2/` |
| `/icons/ds2/weapons/` | `/icons/weapon2/` |
| `/icons/ds2/armors/` | `/icons/armor2/` |
| `/icons/ds2/items/` | `/icons/item2/` |
| `/icons/ds2/magics/` | `/icons/magic2/` |
| `/icons/ds2/dialogues/` | `/icons/dialogue2/` |
| `/icons/ds3/rings/` | `/icons/ring3/` |
| `/icons/ds3/weapons/` | `/icons/weapon3/` |
| `/icons/ds3/armors/` | `/icons/armor3/` |
| `/icons/ds3/items/` | `/icons/item3/` |
| `/icons/ds3/magics/` | `/icons/magic3/` |
| `/icons/ds3/dialogues/` | `/icons/dialogue3/` |

### 2. 执行修复

```bash
node scripts/fixIconPaths.js   # ⚠️ 已废弃删除，请勿再执行
```

**修复结果**：
- ✓ ds1/rings.json
- ✓ ds1/weapons.json
- ✓ ds1/armors.json
- ✓ ds1/items.json
- ✓ ds1/magics.json
- ✓ ds2/rings.json
- ✓ ds2/weapons.json
- ✓ ds2/armors.json
- ✓ ds2/items.json
- ✓ ds2/magics.json
- ✓ ds3/rings.json
- ✓ ds3/weapons.json
- ✓ ds3/armors.json
- ✓ ds3/items.json
- ✓ ds3/magics.json

共修复 15 个数据文件。

---

## 修复前后对比

### 修复前

```json
{
  "id": "100",
  "name": {
    "chn": "生命戒指",
    "jap": "生命の指輪",
    "eng": "Life Ring"
  },
  "icon": "/icons/ds1/rings/100.webp"
}
```

### 修复后

```json
{
  "id": "100",
  "name": {
    "chn": "生命戒指",
    "jap": "生命の指輪",
    "eng": "Life Ring"
  },
  "icon": "/icons/ring1/100.webp"
}
```

---

## 验证

修复后，所有页面的图片都能正常显示：

1. **物品列表页** - 表格中的图标正常显示
2. **物品详情页** - 左侧大图标正常显示
3. **对话页面** - NPC 头像正常显示
4. **收藏页面** - 收藏物品的图标正常显示

---

## 相关文件

### 新增文件
- `scripts/fixIconPaths.js` - 图片路径修复脚本（⚠️ 已废弃删除，由一次性迁移工具完成使命）

### 修改文件
- `src/data/ds1/*.json` - 黑魂1 所有数据文件
- `src/data/ds2/*.json` - 黑魂2 所有数据文件
- `src/data/ds3/*.json` - 黑魂3 所有数据文件

---

## 注意事项

1. **路径格式统一**：所有图片路径都使用 `/icons/{type}{game}/` 格式
2. **文件名保持不变**：只修改路径，不修改文件名
3. **批量处理**：脚本自动处理所有数据文件，无需手动修改
4. **可重复执行**：脚本可以安全地重复执行，不会重复修改

---

## 后续建议

1. **数据生成规范**：在生成数据时直接使用正确的路径格式
2. **路径验证**：添加图片路径验证脚本，确保所有路径都指向存在的文件
3. **自动化测试**：添加图片加载测试，及时发现路径问题

---

最后更新：2026-03-01
